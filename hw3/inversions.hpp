#pragma once
#include <cstddef>
using std::size_t;

template<typename RAIter>
size_t inversions(RAIter first, RAIter last);


template<typename RAIter>
size_t inversions(RAIter first, RAIter last)
{
  return 0;
}
